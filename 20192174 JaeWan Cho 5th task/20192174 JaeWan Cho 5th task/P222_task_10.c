/*#include <stdio.h>

int main()
{
	int x, y;

	printf("x 좌표를 입력하시오: ");
	scanf_s("%d", &x);
	printf("y 좌표를 입력하시오: ");
	scanf_s("%d", &y);

	(x > 0) && (y > 0) ? printf("1사분면") : printf(" \b"); // 좌표를 입력한 후 참이 나오면 몇 사분면에 존재하는지 나오고 거짓이 나오면 아무것도 출력되지 않는다.
	(x < 0) && (y > 0) ? printf("2사분면") : printf(" \b");
	(x < 0) && (y < 0) ? printf("3사분면") : printf(" \b");
	(x > 0) && (y < 0) ? printf("4사분면") : printf(" \b");

	return 0;
}*/