/*#include <stdio.h>

int main()
{
	int first_num, second_num, quotient, remainder;

	printf("2개의 정수를 입력하세요: ");
	scanf_s("%d %d", &first_num, &second_num);
	quotient = first_num / second_num; // 두 수의 몫을 구한다.
	remainder = first_num % second_num; // 두 수의 나머지를 구한다.
	printf("몫: %d 나머지: %d", quotient, remainder);
	
	return 0;
}*/