/*#include <stdio.h>

int main()
{
	int first_n, second_n, third_n, big_n;
	printf("3개의 정수를 입력하세요:");
	scanf_s("%d %d %d", &first_n, &second_n, &third_n);
	
	big_n = first_n > second_n ? first_n : second_n; // 첫번째 숫자와 두번째 숫자를 비교한다.
	big_n = second_n > third_n ? second_n : third_n; // 두번째 숫자와 세번째 숫자를 비교한다.
	big_n = third_n > first_n ? third_n : first_n; // 세번째 숫자와 첫번째 숫자를 비교한다.

	printf("최대값: %d", big_n);

	return 0;
}*/