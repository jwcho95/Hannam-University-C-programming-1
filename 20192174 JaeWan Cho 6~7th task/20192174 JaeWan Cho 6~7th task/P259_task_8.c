/*#include <stdio.h>

int main()
{
	int time, age, money;
	printf("현재 시간과 나이를 입력하시오(시간, 나이): ");
	scanf_s("%d %d", &time, &age);

	if (time < 17) // 시간에 따른 분류
		if ((age >= 3 && age < 13) || age >= 65) // 나이에 따른 분류
			money = 25000;
		else
			money = 34000;
	else
		money = 10000;

	printf("요금은 %d원입니다.", money);

	return 0;
}*/