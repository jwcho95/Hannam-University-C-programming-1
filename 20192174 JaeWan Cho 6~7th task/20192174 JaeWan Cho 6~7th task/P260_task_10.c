/*#include <stdio.h>

int main()
{
	int x, y;

	printf("좌표(x, y): ");
	scanf_s("%d %d", &x, &y);

	if (x > 0 && y > 0)
		printf("1사분면");
	else if (x < 0 && y >0)
		printf("2사분면");
	else if (x < 0 && y < 0)
		printf("3사분면");
	else if (x > 0 && y < 0)
		printf("4사분면");
	else
		printf("어느 사분면에도 속해있지 않음");

	return 0;
}*/