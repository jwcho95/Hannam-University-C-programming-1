/*#include <stdio.h>

int main()
{
	int a_num, b_num, c_num, result;

	printf("3개의 정수를 입력하시오: ");
	scanf_s("%d %d %d", &a_num, &b_num, &c_num);

	if (a_num < b_num) // 첫번째 숫자와 두번째 숫자를 비교한다.
	{
		if (a_num < c_num) // 첫번째 숫자와 세번째 숫자를 비교한다.
			result = a_num;
		else
			result = c_num;
	}
	else
	{
		if (b_num < c_num) // 두번째 숫자와 세번째 숫자를 비교한다.
			result = b_num;
		else
			result = c_num;
	}

	printf("제일 작은 정수는 %d입니다.", result);

	return 0;
}*/